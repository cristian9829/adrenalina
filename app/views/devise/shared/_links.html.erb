<%- if controller_name != 'sessions' %>
	<div class="row">
		<div class="col-md-4" style="margin: 0 auto;">
			<br>
			<div class="container-button" style="display: flex; justify-content: space-around;">
				<strong style="padding-top: 5px;">¿Ya tienes cuenta? </strong>
				<p style="padding-top: 5px">|</p>
		  	<%= link_to "Iniciar Sesion", new_session_path(resource_name), class: "btn btn-light text-primary" %><br />		</div>
		</div>
	</div>
<% end -%>

<div class="row">
	<div class="col-md-4" style="margin: 0 auto;">
		<br>
		<div class="container-button" style="display: flex; justify-content: space-around;">
		<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  		<%= link_to "Registrarse", new_registration_path(resource_name), class: "btn btn-light" %><br />
  		<p style="padding-top: 5px;">|</p>
		<% end -%>
		
		<%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
		  <%= link_to "¿Olvidaste tu contraseña?", new_password_path(resource_name), class: "btn btn-light" %><br />
		<% end -%>			
		</div>
	</div>
</div>

<%- if devise_mapping.confirmable? && controller_name != 'confirmations' %>
  <%= link_to "Didn't receive confirmation instructions?", new_confirmation_path(resource_name) %><br />
<% end -%>

<%- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
  <%= link_to "Didn't receive unlock instructions?", new_unlock_path(resource_name) %><br />
<% end -%>

<%- if devise_mapping.omniauthable? %>
  <%- resource_class.omniauth_providers.each do |provider| %>
    <%= link_to "Sign in with #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider) %><br />
  <% end -%>
<% end -%>
